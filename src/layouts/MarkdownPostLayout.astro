---
import BaseLayout from "./BaseLayout.astro";
import ArticleDetailDisplay from "@/components/BlogContent/ArticleDetailDisplay.astro";
import { fade, slide } from "astro:transitions";
import "../output.css";
type ImageInfo = {
    url: string;
    alt: string;
};
interface FrontMatter {
    title: string;
    pubDate: string;
    description: string;
    author: string;
    images?: Array<ImageInfo> | ImageInfo;
    tags: string[];
}
const { frontmatter, compiledContent } = Astro.props as {
    frontmatter: FrontMatter;
    compiledContent: () => string;
};
---

<BaseLayout>
    <!-- <ArticleDetailDisplay params={frontmatter} name="mainContent"></ArticleDetailDisplay> -->
    <ArticleDetailDisplay
        slot="mainContent"
        params={frontmatter}
        content={compiledContent()}
    />
</BaseLayout>
